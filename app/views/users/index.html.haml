.row
  - if @user
    .well
      = render partial: 'users/card', locals: { user: @user }
  %div
    = page_entries_info @users
  %div
    - @users.each do |user|
      .well
        = render partial: 'users/card', locals: { user: user }
  %div
    = paginate @users
