.well
  = render partial: 'users/card', locals: { user: current_user }
  .row
    = render 'tweets/form'
#timeline
  %div
    = page_entries_info @timeline
  %div
    = render partial: 'tweets/tweets', locals: { tweets: @timeline }
  %div
    = paginate @timeline
