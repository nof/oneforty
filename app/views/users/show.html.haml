.panel.panel-default
  .panel-body= render partial: 'users/card', locals: { user: @user }
%div
  = page_entries_info @tweets
%div
  = render partial: 'tweets/tweets', locals: { tweets: @tweets }
%div
  = paginate @tweets